// Config starter code
import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [createChatBotMessage(`Welcome to PSA's Chat Bot! Ask any questions regarding the data!`)]
}

export default config